import React from "react";
import Form from "../components/Login-Register/Form";

const LoginPage = () => {
  return (
    <main className="main-form">
      <Form name="login" type={1} />
    </main>
  );
};

export default LoginPage;
